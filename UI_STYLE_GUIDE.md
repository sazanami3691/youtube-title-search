# YouTube Title Search UIスタイルガイド

## 目的と適用範囲

PCとiPadで長時間使える、落ち着いたダークテーマの検索PWAを維持するためのプロジェクト規約。ユーザーの明示指定が常に優先される。

## デザイン方針

- 黒に近い背景、濃紺またはチャコールのパネル、明るい枠線、白に近い文字を使う。
- 有効状態と主要操作には落ち着いた緑を使い、危険操作だけ赤系にする。
- 派手なグラデーション、強い光彩、過剰な影を使わない。
- 情報量よりも、検索欄・状態・結果・エラーの読み分けやすさを優先する。

## 基本トークン

| 用途 | 値 |
| --- | --- |
| ページ背景 | `#0d1117` |
| ヘッダー | `#0f141b` |
| メインパネル | `#171d25` |
| コントロール | `#1b232d` |
| 通常枠線 | `#465363` |
| 強調枠線 | `#d5d9e2` |
| メイン文字 | `#f1f3f5` |
| 補助文字 | `#aeb7c2` |
| アクセント緑 | `#4f9d69` |
| 危険操作 | `#c65a5a` |
| 小角丸 | `8px` |
| 標準角丸 | `12px` |

## レイアウト

- `viewport-fit=cover` と `env(safe-area-inset-*)` を使い、iPadのセーフエリアを確保する。
- ページ幅を制限しつつ、狭い画面では1列へ自然に折り返す。
- `min-width: 0`、折り返し、可変画像幅を徹底し、横スクロールを発生させない。
- サムネイルは `aspect-ratio: 16 / 9` と `object-fit: cover` で比率を維持する。
- 長いタイトルは省略せず折り返し、カードからはみ出させない。

## 操作部品

- 入力、ボタン、リンクの主要タッチ領域は44px以上にする。
- ホバーだけに意味を持たせず、通常・フォーカス・押下・無効状態を明示する。
- `:focus-visible` はアクセント色の太いアウトラインで見えるようにする。
- 危険操作は通常操作から距離を取り、色と文言の両方で区別する。
- 通信中は対象操作を無効にし、視覚的な進行表示とテキストを併用する。

## アクセシビリティ

- ラベルを省略せず、状態変化は `aria-live` で通知する。
- カード全体のリンクに分かるラベルを付け、タイトル・サムネイル・ボタンもリンクとして操作できるようにする。
- ダイアログ表示中は開始位置へフォーカスを移し、Escapeと閉じるボタンで戻れるようにする。
- `prefers-reduced-motion` を尊重する。

## 完了前チェック

- [ ] PC幅とiPad相当の狭い幅で横スクロールがない
- [ ] 主要タッチ対象が44px以上
- [ ] 長いタイトルと日本語・英数字混在で崩れない
- [ ] キーボードだけで検索、設定、履歴、結果リンクを操作できる
- [ ] フォーカス、無効、通信中、エラーが色以外でも判別できる
- [ ] APIキー入力は通常マスクされ、キー自体を画面やログへ再表示しない
